Basically a struct `Coordinates` store x and y axis, and a `TrianglePoints` struct stores three `Coordinates`

and encapsulate functions to draw triangles and draw triangle until line is shorter than 1 pixel.

and you can draw font via Font data type.